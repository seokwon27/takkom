
const VaccineBadge = ({name}:{name:string}) => {
  return (
    <div className="px-4 py-1 bg-gray-30 text-gray-500 rounded-full max-sm:text-text-s max-sm:px-2">{name}</div>
  )
}

export default VaccineBadge